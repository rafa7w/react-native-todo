import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        paddingTop: 48,
        borderTopWidth: 1,
        borderColor: '#333333',
        marginLeft: 20,
        marginRight: 20,
        alignItems: 'center',
        justifyContent: 'center'
    }
})